export PATH=~/bin:$PATH
# bun completions
[ -s "/Users/timoptimothy/.bun/_bun" ] && source "/Users/timoptimothy/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"
fish
